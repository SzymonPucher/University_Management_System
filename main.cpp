#include <iostream>
#include "classes.h"
#include <fstream>
#include <cstdlib>
#include <vector>

using namespace std;

int main()
{
    vector < Student > s;
    vector < Professor > p;
    vector < Administrive_Worker > aw;
    vector < Course > c;

    Course ce;
    c.push_back(ce);
    c[0].showCourse();

    return 0;
}
